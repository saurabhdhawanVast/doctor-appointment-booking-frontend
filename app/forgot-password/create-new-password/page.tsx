import React from "react";
import NewPasswordForm from "@/app/components/form_new-Pasword";

const NewPassword = () => {
  return (
    <div>
      <NewPasswordForm token={null} />
    </div>
  );
};

export default NewPassword;
